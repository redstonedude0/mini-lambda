func print_int(x);
func input_int();

func sum(x, y) {
	return x + y;
}

func adder(x) {
	return \(y) -> sum(x, y);
}

func main() {
	a <- 2;
	if (a == 2) then {
		print_int(3);
	} fi
	print_int(a);
}
